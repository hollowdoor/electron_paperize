module.exports = class DOMEvents {
    constructor(element, events){
        this.element = element;
        let listeners = [];
        const on = this.on.bind(this);
        this.on = function(){
            
        };
    }
}
